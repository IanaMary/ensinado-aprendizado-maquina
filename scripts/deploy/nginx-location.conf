# /etc/nginx/sites-enabled/absapt.tk — adicionar DENTRO do server block SSL (443) do absapt.tk
# IMPORTANTE: usar "root" (não "alias") ou ajustar try_files para não duplicar o path

location /h2ia/tutor/ {
    root /var/www;
    try_files $uri $uri/ /h2ia/tutor/index.html;
}
